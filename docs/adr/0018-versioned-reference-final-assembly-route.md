# 采用版本化整机装配参考路线

试点样板路线采用 `START_WIP → ASSEMBLY_BIND → FW_CONFIG → FUNCTION_TEST → FINAL_INSPECTION → COMPLETE`，并将 `PACK_HANDOFF` 作为按生产与仓储责任启用的可选步骤。公开资料能够支持这些阶段所处理的身份、部件、固件、测试和交接事实，但不能证明该站序适用于所有工厂，因此路线必须作为版本化工艺配置而非代码常量；装配与固件写入的先后、测试项目和返工映射在获得现场证据前继续标记为样板假设。
