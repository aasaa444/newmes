# 以迁移和真实 SQL Server 测试保障数据库演进

EF Core Migration 是数据库结构演进的唯一机制，废除 API 启动时的 `EnsureCreated`、结构探测和自动删库重建；部署过程显式执行迁移，API 只校验兼容性。演示种子数据通过显式命令装载，正式环境默认不生成账号、库存或订单。SQLite 测试保留为快速反馈，同时新增临时 SQL Server 上的集成测试，覆盖空库迁移、上一版本带数据升级、约束、事务、幂等和并发。每个可交付版本必须同时证明新装与升级路径，避免以 SQLite 测试通过冒充 SQL Server 可部署性。
