# 管理端采用经典侧栏中后台布局

管理端统一为左侧菜单 + 顶栏（用户/角色/切换过站端）+ 内容区（标题、面包屑、标准列表工具栏）。过站端不套用该壳，保持现场全屏。

二期要解决「不像有菜单的企业系统」与每日操作效率；国内制造/用友金蝶用户对侧栏中后台心智最稳。顶栏主导航二级弱，门户磁贴拉长每日路径。完整设计体系可在此壳内落地，但不单独作为二期唯一交付。

**Consequences**: 所有管理端页面迁入壳内路由；禁止再增无壳散页作为主入口。过站端独立布局与路由前缀保持清晰。
