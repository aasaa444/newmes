# 幂等收件箱与事务发件箱

ERP 入站消息先进入收件箱，以来源、消息 ID、业务键和载荷校验信息实现幂等处理；MES 领域状态与出站消息在同一数据库事务中提交，异步发送器按 `Pending / Processing / Sent / Failed / DeadLetter` 管理重试、错误和回执。集成采用至少一次投递与幂等消费，不宣称严格 exactly-once，并提供人工重试、对账和补偿。ERP 模拟适配器必须经过同一流程；MES 合格完工、仓储接收和 ERP 入账保持为三个可关联但不同的状态。
