# 产品身份来源与分配时机可配置

成品 SN 可以由 ERP、标签系统或 MES 的受控发号适配器提供；MAC、IMEI、证书等标识必须来自拥有分配权的外部系统或受控号池。`START_WIP` 只负责将身份与工单、产品及执行快照正式关联，不再把随机发号写死在首个物理工位。身份分配、绑定、解绑、作废及标签打印/补打均形成制造事件；演示发号适配器只能产生明确标记为非生产的数据，并与真实适配器满足同一接口。
